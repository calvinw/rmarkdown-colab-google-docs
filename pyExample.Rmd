---
title: "Python Example"
jupyter:
  kernelspec:
    display_name: Python 3
    language: python
    name: python3
---


```{python}
import sys
print(sys.version)
```

Declare some variables here:

```{python}
x = 4.34
print(x)
```

Just to make sure variables are still available to other code blocks
(This comes from using reticulate)

```{python}
print(x)
```

Lets import numpy to show we can import modules: 

```{python}
import numpy as np
print(np.__version__)
```

Now lets make an array:

```{python}
y=np.array([[0,1,2,3],
	    [4,5,6,7]])
print(y.ndim)
print(y.shape)
```

Here is some LaTeX:

$$ x = \frac{2}{3} $$

The following is needed to run matplotlib in a notebook, but dont evaluate it:

```{r eval=FALSE}
%matplotlib inline
```
Here is some graphics then:

```{python}
import matplotlib.pyplot as plt
plt.plot([32,31,2,3,5,32],[32,23,3,4,4,32], "o")
plt.show()
```

